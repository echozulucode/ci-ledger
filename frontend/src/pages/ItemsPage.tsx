import Layout from "../components/Layout";

export default function ItemsPage() {
  return (
    <Layout>
      <div style={{ padding: 24 }}>
        <h1>Items Feature Retired</h1>
        <p>The legacy Items page has been replaced by CI Ledger events and inventory views.</p>
        <p>Use the Events, Agents, Tools, and Toolchains sections to manage change records.</p>
      </div>
    </Layout>
  );
}
